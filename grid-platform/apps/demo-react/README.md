# StarGrid Demo (React)

Minimal reference app for the `grid-platform` workspace. Consumes only
`@stargrid/*` packages — no legacy `@starui/*` imports.

```bash
cd grid-platform
npm install
npm run build
npm run dev --workspace=@stargrid/demo-react
```

Open http://localhost:5190
