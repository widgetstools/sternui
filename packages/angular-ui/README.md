# Angular UI Controls (bucket 2)

PrimeNG-themed primitives and `@starui/tokens-primeng` live here when Angular
UI parity work starts. React equivalents are in `packages/react-ui/`.

Do not import from `packages/react-ui/` or `packages/react-core/`.
