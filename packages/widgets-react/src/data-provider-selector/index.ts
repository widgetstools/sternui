/**
 * @marketsui/widgets-react/data-provider-selector — pickers that bind a
 * widget to a saved DataProvider. Reads via `dataProviderConfigService`
 * and surfaces the public/private scope badge and an optional Edit
 * affordance for the selected row.
 */

export { DataProviderSelector } from './DataProviderSelector.js';
export type { DataProviderSelectorProps } from './DataProviderSelector.js';
