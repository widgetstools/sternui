/**
 * Vitest setup for `@starui/config-editor-ui`.
 *
 * The editors are engine-agnostic — tests pass a hand-rolled
 * `ConfigClient` stub via `<ConfigEditorProvider>`, so no IndexedDB
 * shim is required.
 */
