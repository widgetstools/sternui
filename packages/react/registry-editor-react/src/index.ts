/**
 * @starui/registry-editor — legacy barrel.
 *
 * Only `RegistryEditorPanel` remains; `useRegistryEditor` relocated
 * to `@starui/workspace-setup-react` in Task 4. This entire package
 * is deleted in Task 5 once `apps/markets-ui-react-reference` stops
 * referencing `@starui/registry-editor`.
 */

export { RegistryEditorPanel } from './RegistryEditor';
