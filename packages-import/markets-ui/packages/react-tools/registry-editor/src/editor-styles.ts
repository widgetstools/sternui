/**
 * Re-exports the shared editor design system from the dock-editor package.
 * Both editors use the same `--de-*` CSS variables and `[data-dock-editor]` scope.
 */
export { injectEditorStyles } from "@markets/dock-editor";
