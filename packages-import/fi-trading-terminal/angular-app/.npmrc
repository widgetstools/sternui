# Local-only installs — all packages resolved from libs/ tarballs via npm cache.
# First-time setup: run `npm run setup` (populates cache, then installs).
# Subsequent installs: `npm install` works directly from cache.
prefer-offline=true
fetch-retries=0
fetch-retry-mintimeout=100
fetch-retry-maxtimeout=100
