# Customizer modules

Optional MarketsGrid customizer modules extend the side panel:

- `starui_list_grid_features` — built-in toggles
- `starui_add_grid_module` — stub a custom module file
- `starui_explain_grid_feature` — docs for a feature key

Modules run in pipeline order; grid-state should be last.
