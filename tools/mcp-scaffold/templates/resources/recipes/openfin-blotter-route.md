# Recipe: OpenFin blotter route

1. Scaffold `openfin-platform` template.
2. `starui_add_blotter_route` with gridId + componentName.
3. Register view in `manifest.fin.json`.
4. `starui_openfin_launch_checklist` before `npm run client`.
