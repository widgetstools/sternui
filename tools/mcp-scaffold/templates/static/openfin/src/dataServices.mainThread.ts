/**
 * @deprecated Prefer `initPlatformBootstrap()` + `DataHubProvider`.
 * Re-export for legacy imports during migration.
 */
export { getPlatform, initPlatformBootstrap } from './platformBootstrap.js';
