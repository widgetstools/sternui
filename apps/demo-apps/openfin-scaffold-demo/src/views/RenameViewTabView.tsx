/**
 * "Save Tab As…" popout — opened by platform custom action `rename-view-tab`.
 *
 * Re-exports the stock implementation from markets-ui-react-reference pattern.
 * Sets `document.title` on the target view so the tabstrip label updates.
 */
export { default } from './renameViewTab/RenameViewTab';
