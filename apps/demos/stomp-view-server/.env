PORT=8081
ROW_PROFILE=slim
DEFAULT_SNAPSHOT_ROWS=20000
MIN_SNAPSHOT_ROWS=1000
MAX_SNAPSHOT_ROWS=20000
# Live-update frequency: rows mutated per tick (client rate is 1000 ticks/s),
# capped by SWEEP_ROWS_PER_SEC. 200 × cap 60000 ⇒ the full 20k set's
# currentPrice/pnl re-tick ~3×/second.
# DIAGNOSTIC (fix/sharedworker-fanout-blotter-limit): lowered 200→20 to test
# whether per-window client processing (decode + AG Grid apply), not the hub,
# caps concurrent blotters. Restore to 200 after testing.
UPDATES_PER_TICK=20
SWEEP_ROWS_PER_SEC=60000
# DEBUG=1
