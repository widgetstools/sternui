{
  "licenseKey": "openfin-demo-license-key",
  "$comment-background": "Hidden/minimized blotters must NEVER go dormant (trading views: window-local alerting + instant correctness on restore). Three Chromium throttles are disabled: EcoQoS (background renderers demoted to efficiency cores), background timer throttling (setTimeout stretched to 1s when hidden — would stretch the grid's 125ms flush window), and IntensiveWakeUpThrottling (1 wake/minute after 5min hidden — the 'dormant then multi-second catch-up burst' behaviour). --disable-renderer-backgrounding and --disable-backgrounding-occluded-windows keep process priority + occlusion state from downgrading background views. The app-level hidden-pause in useProviderDataWiring was removed for the same policy.",
  "$comment-scrollbars": "Native scrollbars use the platform default (Win11 Fluent: thin rounded thumb that expands on hover) — fully composited, and AG Grid gutters self-size via the grid's runtime scrollbarWidth probe either way. To switch to the classic always-thick square style instead, append FluentScrollbar,FluentOverlayScrollbar,OverlayScrollbar to the ONE comma-separated --disable-features flag (repeated flags don't merge). Arbitrary widths would require ::-webkit-scrollbar styling, which forces main-thread painted scrollbars — deliberately ruled out (see design-system styles/scrollbar.css AG exemption).",
  "runtime": {
    "arguments": "--enable-mesh --security-realm=star-demo --remote-debugging-port=9091 --disable-background-timer-throttling --disable-renderer-backgrounding --disable-backgrounding-occluded-windows --disable-features=UseEcoQoSForBackgroundProcess,IntensiveWakeUpThrottling",
    "version": "43.142.101.2"
  },
  "$comment-devtools": "devtools_port + --remote-debugging-port together expose the OpenFin runtime to Chrome DevTools. Open chrome://inspect#devices in a separate Chrome window and click 'Configure...' → add 'localhost:9091', then SharedWorkers (data-services) appear under 'Other'.",
  "devtools_port": 9091,
  "platform": {
    "uuid": "star-demo",
    "icon": "http://localhost:5175/dock-provider.png",
    "autoShow": false,
    "providerUrl": "http://localhost:5175/#/platform/provider",
    "preventQuitOnLastWindowClosed": true,
    "$comment-fdc3": "Enable the FDC3 API (window.fdc3) for EVERY view/window the platform creates. Without this window.fdc3 is undefined, so the grid color-link transport (useFdc3Channel → useGridContextLink) silently no-ops and color-linked grids never exchange selection context. The dock 'Link' button joins windows to interop context groups; fdc3 broadcast/addContextListener wrap interop.setContext/addContextHandler over that group.",
    "defaultViewOptions": {
      "fdc3InteropApi": "2.0"
    },
    "defaultWindowOptions": {
      "fdc3InteropApi": "2.0"
    }
  },
  "shortcut": {
    "company": "StarUI",
    "description": "Minimal MarketsGrid workspace demo.",
    "icon": "http://localhost:5175/favicon.ico",
    "name": "Star Demo",
    "target": ["desktop", "start-menu"]
  },
  "supportInformation": {
    "company": "StarUI",
    "product": "Star Demo - MarketsGrid Workspace",
    "email": "support@example.com",
    "forwardErrorReports": true
  },
  "customSettings": {
    "appId": "Star-Demo",
    "userId": "k151344",
    "seedConfigUrl": "/seed.json",
    "$comment-useRest": "Master switch for REST mode. true + non-empty configServiceRestUrl → every ConfigManager in the platform writes to the REST server first, mirrors to Dexie, queues pendingSync on transient failures. false (or unset) → local Dexie only. Flip this single boolean to switch the whole platform between local and REST.",
    "useRest": false,
    "configServiceRestUrl": "http://localhost:3001/api/v1",
    "apps": []
  }
}
