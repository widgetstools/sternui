# Star Configuration Service — Environment Variables

# Server
PORT=3001
HOST=localhost
NODE_ENV=development

# Database — SQLite is the only supported engine.
SQLITE_DATABASE_PATH=./data/star-configs.db

# Logging
LOG_LEVEL=info
