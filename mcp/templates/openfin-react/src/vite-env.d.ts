/// <reference types="vite/client" />

// REST mode for the config service lives on the OpenFin manifest's
// `customSettings.{useRest, configServiceRestUrl}` pair, read via
// `getConfigServiceRestUrlFromManifest()` from
// `@starui/openfin-platform/config`.

export {};
